general {
    # Цей параметр гарантує, що при перегляді відео (якщо браузер підтримує)
    # монітори НЕ будуть вимикатися.
    ignore_dbus_inhibit = false
}

listener {
    timeout = 300 
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}
